@model TutorPlatform.ViewModels.HomeIndexViewModel
@{
    ViewData["Title"] = "LiteraSphere";
    var tracks = new[]
    {
        new { Title = "ЕГЭ и итоговое сочинение", Description = "Пробники ЕГЭ, сочинение, аргументация и литературные комментарии в одном блоке.", Tab = "ege-writing" },
        new { Title = "ОГЭ и устное собеседование", Description = "Тестовая часть, изложение, чтение вслух и устные тренировки внутри одного блока.", Tab = "oge-oral" }
    };
}

<section class="hero">
    <div class="hero__content">
        <span class="eyebrow">Платформа репетитора по русскому языку</span>
        <h1>LiteraSphere</h1>
        <p class="hero__lead">
            Единое пространство для репетитора и учеников: авторизация, группы, редактор тестов,
            пробники ОГЭ/ЕГЭ и отслеживание результатов.
        </p>
        <div class="hero__actions">
            @if (User.Identity.IsAuthenticated)
            {
                <a class="btn btn-accent" asp-controller="Home" asp-action="RedirectToDashboard">Открыть кабинет</a>
            }
            else
            {
                <a class="btn btn-accent" asp-area="Identity" asp-page="/Account/Login">Войти</a>
                <a class="btn btn-ghost" asp-area="Identity" asp-page="/Account/Register">Зарегистрировать ученика</a>
            }
        </div>
        <div class="hero__credentials">
            <strong>Демо-доступ:</strong>
            <span>Репетитор: tutor@literasphere.local / Tutor123!</span>
            <span>Ученик: ivan@student.local / Student123!</span>
        </div>
    </div>
    <div class="hero__panel">
        <div class="metric-card">
            <span>Модули</span>
            <strong>4</strong>
            <small>От устного собеседования до сочинения</small>
        </div>
        <div class="metric-card">
            <span>Механики</span>
            <strong>15+</strong>
            <small>Редактор под разные типы тестов</small>
        </div>
        <div class="metric-card">
            <span>Потоки</span>
            <strong>2</strong>
            <small>11 класс и 9 класс</small>
        </div>
    </div>
</section>

<section class="section-grid">
    <div class="section-heading">
        <span class="eyebrow">Основные разделы</span>
        <h2>Перейти в нужный трек подготовки</h2>
    </div>
    <div class="cards cards--compact">
        @foreach (var track in tracks)
        {
            var href = User.Identity.IsAuthenticated
                ? Url.Action("RedirectToDashboard", "Home", new { tab = track.Tab })
                : Url.Page("/Account/Login", new { area = "Identity" });
            <a class="info-card info-card--dark info-card--link" href="@href">
                <h3>@track.Title</h3>
                <p>@track.Description</p>
            </a>
        }
    </div>
</section>
